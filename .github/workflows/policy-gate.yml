name: Policy Gate

on:
  workflow_dispatch:
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  pull-requests: read

jobs:
  policy-gate:
    name: policy-gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Enforce layered fix PR policy
        shell: bash
        env:
          HEAD_REF: ${{ github.head_ref }}
          BASE_REF: ${{ github.base_ref }}
          PR_LABELS: ${{ toJson(github.event.pull_request.labels.*.name) }}
        run: |
          set -euo pipefail

          echo "HEAD_REF=$HEAD_REF"
          echo "BASE_REF=$BASE_REF"
          echo "PR_LABELS=$PR_LABELS"

          if [[ "$HEAD_REF" == fix/* ]]; then
            if [[ "$BASE_REF" == "main" || "$BASE_REF" == "master" ]]; then
              if ! echo "$PR_LABELS" | jq -e '. | any(. == "layered-pr-exception")' > /dev/null; then
                echo "ERROR: fix/* PRs must target a layered branch, not main/master."
                echo "Use stack/*, layer/*, release/*, or add label layered-pr-exception."
                exit 1
              fi
            fi
          fi

          git fetch origin "$BASE_REF" --depth=1 || true
          PR_HEAD="${{ github.event.pull_request.head.sha }}"
                    MERGES=$(git rev-list --merges "origin/$BASE_REF..$PR_HEAD" || true)
          if [[ -n "$MERGES" ]]; then
            if ! echo "$PR_LABELS" | jq -e '. | any(. == "allow-merges")' > /dev/null; then
              echo "ERROR: merge commits detected in PR diff range:"
              echo "$MERGES"
              echo "Add label allow-merges to bypass if rebase is not allowed."
              exit 1
            fi
          fi


          echo "Policy gate passed."
