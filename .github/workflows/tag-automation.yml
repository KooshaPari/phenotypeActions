name: Tag Automation
on:
  workflow_dispatch:
jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Create release tag
        env:
          REF_NAME: ${{ github.ref_name }}
        run: echo "Creating release for $REF_NAME"
