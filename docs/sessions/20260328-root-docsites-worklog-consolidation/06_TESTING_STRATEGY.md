# Testing Strategy

## Validation

- Verify the session bundle has the canonical file set.
- Confirm the docs tree still builds if the repo uses VitePress or similar docs tooling.
- Keep the validation lightweight and docs-focused.

